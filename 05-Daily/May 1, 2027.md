add storage capacity constraints for specialization. In **NODE_REST** add new columns for specialization so that if a node can receive a specialized node then a capacity constraint for that node specialization is need and that column for the po line has to be non-empty and non-null. If you don't want to enforce capacity constraint for DG then add -1. -1 tells us this capacity constraint for DG at this node is off.


below 1 and 2 don't make sense. If storage-type restriction is not met can't store there. It'll have to go to a location that can store this type. If over-capacitated then over c penaly applies.
1.  Mtype Penalty weight per CBM assigned to a node whose storage-type restriction is violated. Default: 10,000.
2. Mspec Penalty weight per CBM assigned to a node whose specialization certification is violated. Default: 1,000.

3.5 Derived binary indicators (computed during preprocessing) - don't do what is suggesting in the latex file. What you should do is create a list of valid nodes that each po-line can go to that satisfy both storage-type and specialization requirements. 

5 Objective function (1) should only have one term the M^{cap} first term (summation). Remove the other two.  Define another penalty term that penalized for infesibility (this is only used if there is not a single node in the network that can take the po-line becuase there isn't any node that matches the capcity type and specialization requirements)

